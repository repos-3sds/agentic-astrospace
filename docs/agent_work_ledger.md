# Agent work ledger

Status: living state, not a spec. Changes daily/hourly. The rules that
govern *how* to use this file live in [AGENTS.md](../AGENTS.md) — this file
only records current reality: who's doing what, on what branch, touching
what. Stale rows (no update in a while, work clearly finished/abandoned)
should be pruned, not archived — this isn't a history log, `git log` is.

## How to use this

- **Starting work?** Add or update your row before you start, per AGENTS.md
  Rule 1 — especially the "touching" column if it reaches outside your own
  owned area.
- **Stopping mid-task?** Update status/notes before you stop, per Rule 2 and
  Rule 6 — don't leave a row saying "in progress" for work that's actually
  finished or abandoned.
- **About to touch a file outside your own area?** Check this table first
  for whether someone else's row already claims it.

## Current assignments

Full task specs for the current round: [docs/agent_task_briefs_2026-08-09.md](agent_task_briefs_2026-08-09.md)
(supersedes [the 2026-08-08 briefs](agent_task_briefs_2026-08-08.md) for the
rows below — that doc's Claude/Codex/Gemini/Qwen sections are all now
closed out). This table stays intentionally thin — status only, detail
lives in the brief.

Round assigned by Claude 2026-08-09 after PR #10 merged. All four rows
below closed out same day — see the open-handoffs note below on how
Qwen's row actually got closed (not by Qwen).

| Agent | Task | Branch | Status | Touching | Last updated |
|---|---|---|---|---|---|
| Codex | Profile Context Ledger epic: define the consent, provenance, correction/deletion, confidence, temporal validity, cross-profile isolation, retrieval, and Ask integration contract for remembered life facts. Architecture/spec only in this slice; no backend schema, agent prompt, registry, or production persistence changes without Claude review. | `codex-profile-context-ledger` | Contract complete; blocked at reviewed DB/orchestrator/native-storage implementation boundary | `docs/profile_context_ledger_architecture_2026-08-14.md`, ledger | 2026-08-14 |
| Codex | Reliable Native Core epic, Phase 0/1: architecture contract plus a bounded local-first Today + Calendar pilot. Canonical envelopes now isolate account/profile/revision/place/conventions/persona depth; fresh values avoid redundant route loads; stale values render with a notice and refresh once; corrupt/expired and legacy unscoped rows are discarded; profile/logout invalidation is centralized. This is the WebView-storage adapter, not the final encrypted native database. | `codex-reliable-native-core` | Runtime pilot complete in PR #51; encrypted SQLite/native lifecycle slice requires dependency review and Android+iOS proof | `docs/reliable_native_core_architecture_2026-08-14.md`, `ui/src/app/core/resource-cache*`, `ui/src/app/core/vedic.service*`, Today/Calendar components | 2026-08-14 |
| Codex | Today birth-signature visual alignment — **DONE, PR #47 pending**. Reconciled the newly merged Birth Details card with the older `ALWAYS · YOUR SIGNATURE` grid into one token-driven `YOUR BIRTH SIGNATURE` surface: birth time/place, Lagna, Rashi, Nakshatra, lucky number, gem, and direction now share one hierarchy without a nested card. Added an explicit reusable `embedded` presentation to `BirthDetailsCardComponent`, content-weighted fact columns, and no mid-word wrapping. Device-review follow-ups standardized Today's vertical rhythm (12px section-label clearance; 7–8px label-to-value spacing) and moved the signature from an opaque accent fill onto the exact light/dark glass recipe used by `What Matters Today`; accent remains only in hierarchy cues. Verified 46/46 frontend tests, production build, and 375x812 live-data screenshots in light/dark after merging current `main`. | `codex-today-birth-signature` | Done; PR #47 pending review | `ui/src/app/features/mobile/today/today.component.{ts,html,scss}`, `ui/src/app/features/mobile/chart/birth-details-card.component.*`, Today visual evidence | 2026-08-13 |
| Codex | Admin Agent Prompt Registry — **DONE, PR pending review**. Added a full-admin-only endpoint sourced directly from `_BASE_SYSTEM`, `AGENT_REGISTRY`, and the CE taxonomy; added a read-only console view with live coverage, prompt composition inputs, configured/unconfigured domains, domain switching, source symbols, and copy action. Browser-verified in light/dark; caught and fixed a runtime icon-provider failure and clipped coverage layout. No browser editing or registry mutation. Focused backend: 9/9; frontend: 31/31; production build clean with no budget warning. | `codex-admin-agent-prompt-registry` | Done | `astrospace/api/admin_routes.py`, `ui/src/app/core/admin.service.ts`, `ui/src/app/features/admin/*`, focused admin tests | 2026-08-10 |
| Claude | Ask backend Item 3: tense-aware context — **DONE, merged** (PR #12). `detect_tense()`, deterministic `profile_facts` block, domain-agent prompt rule, verifier invariant for invented future timelines. Went through 2 rounds of independent subagent review (no other agent was active to review live) — found and fixed 3 critical bugs (verifier flagging the bundle's own real dasha period boundaries; a future-phrase regex firing on ordinary closes; tense classification collapsing genuinely two-part questions and feeding a blocking invariant) plus a field-coverage gap (prohibited-verdict checks missing `acknowledgment`/`remedies`/`follow_up_questions`). Full suite: 1056 passed, 10 skipped at merge | `main` | Done | `astrospace/agents/intent.py`, `astrospace/agents/domain_agent.py`, `astrospace/agents/orchestrator.py`, `astrospace/agents/verifier.py`, `astrospace/context/assembler.py` | 2026-08-09 |
| Codex | Mobile screen build backlog — **PR #13 done, merged**: Ask Answer persona templates (Guided/Balanced/Practitioner), verified live + screenshotted. One review finding fixed before merge: `modeEyebrow()` printed the literal label "DECISION VERDICT"/"SYNTHESISED VERDICT" on every answer, colliding with design_principles.md §4's "flag, not a verdict" hard constraint — changed to "READING SUMMARY"/"CHART SYNTHESIS". Offline/stale/partial-calculation/unknown-birth-time/notification-denied states and the rest of the ❌ list remain queued, not started | — | Done (this slice); mobile backlog otherwise open | `ui/src/app/features/mobile/ask/*` | 2026-08-09 |
| Codex | Full Ask mobile regression sweep — **DONE, audit only**. Tested Android plus 375x812 local light/dark across Guided/Balanced/Practitioner, two-profile switching, new questions, follow-ups, reopening, history swipe/archive, back behavior, structured response depth, terminal states, Copy/Edit, and production build. Report records 1 P0, 5 P1, 9 P2, and 2 P3 findings. Highest risks: configured Postgres rejects three application safety kinds (`death`/`health`/`money` vs legacy constraint values), cached Answer state crosses profile/thread boundaries, and topic/tone controls are not wired to the Ask request. No fixes included | `codex-ask-full-regression` | Done; ready for review | `docs/ask_full_regression_audit_2026-08-10.md`, `docs/ask_regression_evidence/2026-08-10/*` | 2026-08-10 |
| Codex | Native mobile release blockers: auth-session persistence, location/calendar invalidation and latency, dark-mode Ask icons, WebView drag/link behavior, stale route retention, duplicate Ask streaming state, and Get Started leaking during session restoration. ASK-002/003 integrated. Practitioner Yantra now has a prominent Ask Siddha gateway; Practitioner Ask is a focused subflow with no footer and returns to Yantra. Build 236 installed and the complete Yantra -> Ask -> Yantra flow verified on Samsung. 44/44 UI tests, production build, release build/signature, and diff checks pass. | `codex-mobile-native-regression` | Done; PR #29 awaiting review | `ui/src/app/core/*`, `ui/src/app/features/mobile/*`, global mobile styles; no native project source churn | 2026-08-11 |
| Codex | PR #37 integration fix — **DONE**: Birth Details is now the first normal Practitioner Yantra tile beneath the full-width Ask Siddha gateway, paired with Charts & Vargas. Added a focused ordering/route regression test. Verified Yantra -> Birth Details -> Yantra at 375x812 in light/dark using the local QA profile, including the full calculation panel and zero console errors. Frontend 46/46; production build clean. | `codex-pr37-practitioner-gateway` | Done; commit pushed to PR #37 head | `ui/src/app/features/mobile/chart/chart-hub.component.ts`, `chart-hub.component.spec.ts` | 2026-08-12 |
| Claude | Ask backend Item 3: tense-aware context — **DONE, merged** (PR #12). `detect_tense()`, deterministic `profile_facts` block, domain-agent prompt rule, verifier invariant for invented future timelines. Went through 2 rounds of independent subagent review (no other agent was active to review live) — found and fixed 3 critical bugs (verifier flagging the bundle's own real dasha period boundaries; a future-phrase regex firing on ordinary closes; tense classification collapsing genuinely two-part questions and feeding a blocking invariant) plus a field-coverage gap (prohibited-verdict checks missing `acknowledgment`/`remedies`/`follow_up_questions`). Full suite: 1056 passed, 10 skipped at merge | `main` | Done | `astrospace/agents/intent.py`, `astrospace/agents/domain_agent.py`, `astrospace/agents/orchestrator.py`, `astrospace/agents/verifier.py`, `astrospace/context/assembler.py` | 2026-08-09 |
| Codex | Mobile screen build backlog — **PR #13 done, merged**: Ask Answer persona templates (Guided/Balanced/Practitioner), verified live + screenshotted. One review finding fixed before merge: `modeEyebrow()` printed the literal label "DECISION VERDICT"/"SYNTHESISED VERDICT" on every answer, colliding with design_principles.md §4's "flag, not a verdict" hard constraint — changed to "READING SUMMARY"/"CHART SYNTHESIS". Offline/stale/partial-calculation/unknown-birth-time/notification-denied states and the rest of the ❌ list remain queued, not started | — | Done (this slice); mobile backlog otherwise open | `ui/src/app/features/mobile/ask/*` | 2026-08-09 |
| Codex | ASK-002/ASK-003 profile isolation and new-thread lifecycle — **DONE, PR #25 awaiting review**. Route-less Ask submissions cannot inherit a cached thread; fetched threads are profile-verified before any messages or session cache render; profile changes abort and clear active work; late stream frames and stale A/B thread fetches cannot repaint. Six attacker-side race/isolation tests, 40/40 frontend tests, production build, and 375x812 browser pass. PR #24 reviewed and merged; PR #19 updated with the missing persona `register` runtime input plus contract assertion. | `codex-ask-profile-isolation` | Done; PR review pending | `ui/src/app/features/mobile/ask/ask-answer.component.*`, `ui/src/app/core/models.ts` | 2026-08-11 |
| Gemini | Health domain — **DONE, merged** (PR #11). No new task queued | `main` | Done | `astrospace/agents/registry.py`, `tests/*` | 2026-08-09 |
| Qwen | Adversarial audit extended to Wealth + Children dosha/prohibited-verdict phrasing — **task complete, but not by Qwen.** Her environment couldn't push (no git remote credentials, then a patch that didn't match this repo's real files — fabricated hashes, a nonexistent `ledger.txt`, non-functional Python). Claude independently confirmed the same 20-phrase gap class was real, then did the audit + fix directly: **PR #14, merged**, 6 rounds of independent subagent review (the deepest review chain this session — see architecture doc if it exists there, otherwise `git log` on `claude/wealth-children-fatalism-audit`'s squashed history). Root mechanism changed twice: generic verb×subject windowing → explicit phrase patterns (round 3) → splitting negation-checked wealth/children patterns from bare-search marriage patterns after sharing one negation check regressed marriage twice (round 5). Full suite: 1193 passed, 2 skipped at final merge | — | Done | `astrospace/agents/safety.py`, `tests/test_verifier.py` | 2026-08-09 |
| Claude | Foreign Travel & Settlement domain — **DONE, merged** (PR #15). User-requested (career/marriage/wealth/children/health had it, foreign travel/immigration/settlement was the real gap). Taxonomy entry (`domains.foreign`) was already fully specified; wired via `_FOREIGN_ADDENDUM` in `registry.py`, plus one taxonomy fix (bare `"settle"` keyword narrowed to `"settle abroad"/"settle overseas"` — was misrouting `"settle down"` marriage questions with no ambiguity signal to trigger clarification). Wiring the domain up turns immigration-outcome questions into live-agent answers instead of a safe refusal, so `safety.py`'s immigration vocabulary needed to become genuinely comprehensive — went through **5 rounds of independent subagent review**, each finding real, demonstrated gaps (round 1: one exact phrasing only; round 2: regex suffix bug leaving denied/rejected/granted/accepted with no bare form, get/receive family missing; round 3: noun/adjective outcome forms + output-net framing drift between arms; round 4: the round-3 noun fix only covered half its own verb families, residency-narrowing fix recreated its own medical/tax collision one word later; round 5: output net missed hedged/probabilistic certainty language — "has a high chance of approval", "probability...is 90%" — plus USCIS/DACA/TPS/petition subject terms and the residency fix being US-only for a domain that's explicitly global). Every round's fix verified via an adversarial probe script before being written into `tests/test_refer_out_boundary.py` as permanent regression tests. One documented residual limitation left deliberately unfixed per the round-4/5 reviewers' explicit recommendation (a low-severity "SUBJECT is guaranteed to &lt;unrelated&gt;" false positive — the fix would reopen a real catch). Full suite: 1305 passed, 10 skipped at merge | `main` | Done | `astrospace/agents/registry.py`, `astrospace/agents/safety.py`, `astrospace/context/taxonomy.json`, `tests/test_refer_out_boundary.py`, `tests/test_context_engine.py`, `tests/test_domain_agent.py`, `tests/test_agent_registry.py` | 2026-08-09 |
| Claude | Ask persona-mode depth/timing fix — **DONE, merged** (PR #17). User (product owner) reviewed live behavior and flagged two problems: (1) Guided mode was truncating the explanation itself (190-char `interpretation` via `compactSentence`, actions `.slice(0,2)`, no technical basis, `guidance.remedies` never rendered in the structured view at all) instead of only hiding technical depth — full explanation is now common to Guided/Balanced/Practitioner (identical `interpretation`, new full `summary_and_assurance` paragraph, unsliced `practical_actions` via a shared `readingActions()`, a new "Traditional supports" remedies block), and only `technical_basis` disclosure now varies (Guided: new one-line `guidedTechnicalHint()` or nothing; Balanced: existing `.slice(0,3)` subset, now gated through the previously-unused `showTechnicalBasis()`; Practitioner: full list, unchanged); confidence now shows in Guided too (explanation content, not technical disclosure, per spec). Touched `ui/src/app/features/mobile/ask/*` directly (Codex's owned area per Rule 4) per explicit user instruction in this task's brief — flagged here since it's outside my usual area, no objection raised. (2) Backend: `domain_agent.py`'s ~350-word cap removed (completeness over brevity); new TIMING PRECISION rule requires citing real `dasha_relevance`/`gochara` dates for timing-shaped questions, worded explicitly to keep a period noun next to any remaining-duration number (matches `safety.py`'s existing death-timing guard, not touched) and to keep a favourability window distinct from a deterministic outcome; `assembler.py`'s `_gochara_for_domain()` now carries real `start_date`/`end_date` per active transit rule by reusing `gocharam/timeline.py`'s existing `gocharam_rule_timeline()`, not reimplementing transit math. One round of independent subagent review (fresh context, checked specifically for safety-boundary regressions and spec compliance) found one real, demonstrated blocking bug — `verifier.py`'s `_period_boundary_years()` only exempted `dasha_relevance.chain` years from the retrospective-tense "invented future timeline" check, not the new `gochara.active_rules[]` years, so a genuinely bundle-grounded gochara date could get falsely flagged; reproduced directly against a real fixture and fixed — plus two non-blocking gaps fixed same round: `base.py`'s structured-reading `max_tokens` (4096, hardcoded) risked truncating the tool-call JSON once the word cap was removed, bumped to 8192 on the two structured-reading call sites only; no `.spec.ts` existed for `ask-answer.component` at all, added one covering the mode-dependent methods. Verified live via `ng serve` + local backend (`ASTROSPACE_DEV_AUTH_BYPASS`) across career/foreign/marriage domains and all three modes — confirmed identical explanation content across modes, real dasha/gochara dates narrated as favourability windows (not outcomes), and "will I get my visa approved" still correctly hitting the existing refer-out gate. Full suite at merge: 1424 passed, 10 skipped (the 8 extra skips vs. the 1430/2 baseline are a pre-existing, unrelated environment gap — this worktree has no local knowledge-base corpus for `test_knowledge_ingestion.py`, confirmed absent in the main checkout too, not caused by this change). Frontend: 30/30 | `main` | Done | `ui/src/app/features/mobile/ask/ask-answer.component.ts`, `ui/src/app/features/mobile/ask/ask-answer.component.html`, `ui/src/app/features/mobile/ask/ask-answer.component.scss`, `ui/src/app/features/mobile/ask/ask-answer.component.spec.ts`, `astrospace/agents/domain_agent.py`, `astrospace/agents/base.py`, `astrospace/agents/verifier.py`, `astrospace/agents/registry.py`, `astrospace/context/assembler.py`, `tests/test_context_engine.py`, `tests/test_verifier.py` | 2026-08-10 |
| Claude | Personality & Self-Understanding domain — **DONE, merged** (PR #16). User-requested: none of the 10 shipped domains cover self-analysis ("what are my strengths/weaknesses", "what's my nature/temperament", "how's my emotional intelligence") — structurally different from every other domain (all 10 answer "what will happen in life-area X"; this one answers "what am I like"). Unlike every prior domain this session, no pre-authored taxonomy spec existed — authored from scratch against the classical corpus and this codebase's actual engine (checked `knowledge/vedic_rules/yogas.json`/`doshas.json` before citing any rule_id): Lagna (house 1) primary, houses 3/5 secondary, Sun/Moon/Mercury + Jaimini Atmakaraka (AK) as karakas, D1-only (whole-chart-character domain, not a divisional-chart one), Pancha Mahapurusha + four other yoga categories, `gandanta_dosha` pulled in by rule_id since its category isn't otherwise selected. Resolved the task's flagged open design question directly rather than deferring it: the Ascendant lord needed no `assembler.py` change — house 1's own `lord`/`lord_placement` already surfaces it, same mechanism every domain uses for primary houses; pinned with a dedicated test. New `_PERSONALITY_OVERCLAIM_OUTPUT` guardrail added to `safety.py` (character-fatalism, explicit phrase/shape matching per this file's own documented history of generic verb×subject windowing failing twice for wealth/children). **Two rounds of independent subagent review** (this domain sits directly adjacent to the mental-health refer-out boundary, treated as more safety-sensitive than average, not less) — both rounds run from scratch with no memory of the implementation, and both converged on the same real gaps: (1) routing — `"strengths and weaknesses"` keyword was redundant with `"my strengths"`, double-counting one mention into `confidence="high"`, compounded by a separate pre-existing orchestrator bug (`_needs_clarification()` never ran its tie check above `"medium"` confidence) that let personality silently outrank real one-keyword competitors from career/wealth/children/marriage/spirituality — fixed both (dropped the redundant keyword; generalized the tie check to run at any confidence level with a real secondary domain present), plus a one-word unrelated fix (`exam` keyword didn't match plural `exams`); (2) safety guardrail — the original 8-adjective character-fatalism vocabulary missed everyday traits and whole sentence shapes ("you will always be lazy", "once a manipulator, always a manipulator" both passed clean), widened substantially including a generalizing backreference pattern (`once a X, always a X`); clinical/psychiatric vocabulary the addendum prohibits had zero output-side enforcement, added as health-kind `_PROHIBITED_OUTPUT` entries; `refer_out_kind()`'s health subject list had no anxiety/panic/mood vocabulary at all (pre-existing `safety.py` gap, newly reachable via this domain's own vocabulary), added within the existing subject+verdict-frame design. Every finding demonstrated failing pre-fix via an adversarial probe script before being written into `tests/test_context_engine.py`/`test_domain_agent.py`/`test_verifier.py`/`test_refer_out_boundary.py` as permanent regression tests. One residual limitation documented and pinned rather than chased further (fully open self-reflective emotional-distress phrasing with no explicit verdict frame — widening the shared `_VERDICT_FRAMES` list would raise false-positive risk across every domain it gates, not just this one). Full suite: 1422 passed, 10 skipped at merge | `main` | Done | `astrospace/agents/registry.py`, `astrospace/agents/safety.py`, `astrospace/agents/orchestrator.py`, `astrospace/context/taxonomy.json`, `tests/test_context_engine.py`, `tests/test_domain_agent.py`, `tests/test_agent_registry.py`, `tests/test_verifier.py`, `tests/test_refer_out_boundary.py` | 2026-08-09 |

| Claude | Consultation validation loop, wealth first — **built, pushed to `claude/wealth-validation-loop-w0g7os`, not merged.** Scoped by `docs/handoff_validation_loop_2026-08-10.md` §1 items 1-3 (item 4, birth-time rectification, deliberately untouched). Blocker first: `safety.py`'s death cluster was anchored entirely to "you", re-measured at 10/12 third-party probes missed on shipped code, closed before anything stores life events. Then `context/timeline.py` (four overlapping dated windows flattened into one sorted, aged, status-marked list), `context/validation.py` (engine-side ambiguity slots for wealth + scoring + the `life_context` section), `db/models.py::ValidationProbe` + migration `20260810120000`, `agents/validation_agent.py`, and the `validation_needed` turn through orchestrator and route with a `POST /api/v1/ask/validation/{probe_id}/answer` endpoint. Two real bugs found by checking rather than trusting: the slot picker anchored questions to the whole current mahadasha, which for one test chart meant asking about 2021-2041 (fifteen years that have not happened) and for another about a window starting at age 2 — both now clamped and pinned. Handoff §4's guess that `PredictionClaim` was reusable turned out wrong (NOT NULL `reading_id` into `readings`, which an Ask-side probe has no row in); a new table carries it, mechanism reused. Ships **off** — `AskRequest.validate_first` defaults False until the mobile Ask renderer handles the envelope (Codex's area); answered probes feed `life_context` regardless. **Review round 1 (PR #20, independent adversarial reviewer): 3 blocking, 2 high, 3 medium, all fixed.** The commit-before-ask invariant itself held under direct attack (canary claim reached neither envelope, evidence, nor life_context; `_history_from_thread` replays only role/content) — no design change, all findings were surrounding code. B1: a combined guard clause (`if key in seen or slot_id in exclude`) skipped `seen.add()` for excluded slots, so answering a question unmasked its suppressed twin. B2: a rejected probe insert left the request session in a failed transaction and the reader got `fatal_error` instead of their reading — rollback moved into the db layer. B3+H2: the lifespan patterns matched the verb rather than its object and flagged "live to see", "live to enjoy", "live beyond his means"; rebuilt around `_LIFESPAN_OBJECT` with first- and third-person sharing one subject. H1+M3: the age floor had reached one generator of three (8% of emitted slots still anchored before age 14) — moved into `_anchor_from`, which every generator goes through, together with a 10-year maximum window. M1: answer keys now validated against the probe's own options. M2: probes scoped by domain so a wealth disclosure cannot reach a marriage or health bundle. Verification method changed on the reviewer's advice: adversarial sets written from the app's own vocabulary rather than paraphrases of the positive cases, which found 4 more issues than the review listed — including a PRE-EXISTING false positive in `main` ("you have 2 years left in this Saturn dasha" read as a death verdict). Every fix verified by reverting it and confirming the new test goes red. The intermittent test is resolved: it was the only test in its file not stubbing the model call, so it passed without provider credentials (fail-open swallowed the raise) and failed with them; an autouse fixture now makes that class un-writable in that file. The 1616/1617 discrepancy was a stale number I transcribed into this ledger from a run before the last test was added — 1617 was correct. Full suite after merging main: 1671 passed, 10 skipped, 1 xfailed. Previously: 1616 passed, 10 skipped, 1 xfailed | `claude/wealth-validation-loop-w0g7os` (PR #20) | Review round 1 addressed, re-pushed | `astrospace/agents/safety.py`, `astrospace/agents/orchestrator.py`, `astrospace/agents/domain_agent.py`, `astrospace/agents/validation_agent.py`, `astrospace/context/{assembler,timeline,validation}.py`, `astrospace/db/{models,crud_mobile}.py`, `astrospace/api/{ask_routes,ask_stream_routes}.py`, `supabase/migrations/20260810120000_*.sql`, `tests/{test_validation_loop,test_refer_out_boundary}.py` | 2026-08-10 |

## Open handoffs / things the next agent in an area should know

- **Validation loop is on a branch, unmerged, and ships behind a flag**
  (Claude, 2026-08-10). `claude/wealth-validation-loop-w0g7os` touches
  `registry`-adjacent and schema-shaped things (a new table + migration), so
  per Rule 5 it wants review before merge rather than a self-merge. Two
  things the reviewer should look at first: the commit-before-ask ordering
  (it is the entire feature, and it is enforced structurally — see the
  architecture doc's Update 2026-08-10), and the fact that
  `AskRequest.validate_first` defaults False, so merging changes no shipped
  behaviour except that answered probes would start reaching readings as
  `life_context`. **Codex:** flipping it on needs the mobile Ask renderer to
  handle a `validation_needed` envelope (options as chips, a skip
  affordance, then re-send the original question) — that is the only thing
  standing between this and being live.
- **`AGENTS.md` Rule 3b (branch switches in the shared working directory)
  is confirmed and live** — merged via PR #5, 2026-08-08. Full incident
  history that produced it isn't repeated here anymore (it's fully
  resolved, and it's in `git log`/the merged PRs if anyone needs it) — the
  rule itself is what matters going forward: use `git worktree add <path>
  <branch>` instead of `git checkout`/`git checkout -b` in this directory.
- **`astrospace/admin/client.py`** (Codex-owned per AGENTS.md): had a live
  uncommitted-edit collision with Claude's session on 2026-08-08 — already
  resolved (Codex committed the fix, `986963a`), but worth knowing this file
  had a rocky day before touching it again.
- **Ask backend hardening sequence** (Claude-owned): the six-item priority
  list in the architecture doc's Update section is meant to run in order —
  item 3 (verifier) depends on item 4's context-trimming work not shipping
  first, per the dependency note already written there. Don't parallelize
  those items across agents; they're sequential by design.
- **PR #1** (Qwen, merged 2026-08-08): added `CONTEXT_ENGINE_MULTI_AGENT_AUDIT_2026-08-08.md`
  at repo root and rewrote `.gitignore`. The gitignore rewrite dropped three
  exclusions without noticing why they existed; Claude restored them same
  day (`9be8011`). If Qwen's next task touches `.gitignore` again, read the
  restored comments first.
- **`AGENTS.md` is now live (merged via PR #2, 2026-08-08, reviewed and
  approved by Codex).** It's the real operating protocol as of now, not a
  draft — everyone (including Gemini and Qwen, before either gets a first
  task) reads it, claims work here before starting, and treats config/
  registry/infra edits and non-trivial feature work as PR+review by default.
  The one correction from review worth flagging specifically: a new
  `AGENT_REGISTRY` entry is *not* low-risk — it's a production trust
  boundary and needs owner review plus the career/marriage-level test bar,
  not a quick append.
